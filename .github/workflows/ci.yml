name: CI
on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: salon_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U test" --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      # фиктивные значения: реальные секреты в CI не нужны
      DATABASE_URL: postgresql://test:test@localhost:5432/salon_test
      DIRECT_URL: postgresql://test:test@localhost:5432/salon_test
      TEST_DATABASE_URL: postgresql://test:test@localhost:5432/salon_test
      MAX_WEBHOOK_SECRET: ci_secret_value
      CRON_SECRET: ci_cron_secret_value_123456
      ADMIN_SESSION_SECRET: ci_admin_session_secret_0123456789abcdef
      SALON_TIMEZONE: Asia/Yekaterinburg
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma migrate deploy
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build
