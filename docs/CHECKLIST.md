# Чек-лист готовности (по разделам 54 и 84 ТЗ)

Обозначения: ✅ реализовано в коде и (где возможно) проверено автономными тестами · 🔎 нужно проверить вам на живых сервисах · ⚠️ требует вашего действия · ❌ не сделано / сознательно иначе.

## Раздел 54 — production
| Пункт | Статус | Комментарий |
|---|---|---|
| Токен MAX / ключ OpenAI / ключ Google не в Git | ✅ ⚠️ | `.gitignore` закрывает `.env*` и ключи; перед первым push проверьте `git status` |
| Webhook по HTTPS, секрет проверяется | ✅ | HTTPS даёт Vercel; `max:setup` принимает только `https://`; тест на `401` |
| Актуальный endpoint MAX, заголовок `Authorization` | ✅ | `platform-api2.max.ru`, токен только в заголовке |
| PostgreSQL работает | 🔎 | миграции создаёте вы (`prisma migrate dev`) |
| Redis работает | ❌ | не используется: очереди в PostgreSQL + Vercel Cron (требование про Vercel имеет приоритет) |
| Google Sheets работает | 🔎 | `/admin/sheets`, `health?deep=1` |
| Двойная запись невозможна | ✅ 🔎 | три уровня защиты; интеграционный тест |
| Отмена, перенос | ✅ 🔎 | код и интеграционные тесты |
| Напоминания | ✅ 🔎 | без дублей (уникальность + атомарный захват); тесты отправки |
| Админка защищена, роли | ✅ 🔎 | тесты матрицы прав, сессий, ограничения по роли |
| Логи, обработка ошибок | ✅ | структурные логи без секретов; пользователю показываются нейтральные сообщения |
| Тесты проходят | 🔎 | автономно выполнено 65 проверок логики (слоты, защита AI, синхронизация с таблицей, права, расписание, кнопки и др.) и статический разбор типов со схемой БД; остальные запускаете вы (`npm test`, CI) |
| README, `.env.example` | ✅ | |
| Docker | ❌ | Dockerfile не нужен на Vercel; `docker-compose.yml` — только локальная БД |

## Раздел 84 — Vercel
| Пункт | Статус |
|---|---|
| Next.js App Router, Node.js runtime | ✅ (`export const runtime = "nodejs"` у API-маршрутов) |
| Нет `app.listen()`, постоянного процесса, `setInterval`/`setTimeout` для критичных задач | ✅ (проверено поиском по коду: `listen`/`setInterval` нет; три `setTimeout` — паузы повтора HTTP-запроса ≤3 с внутри одного вызова, не планирование задач) |
| Нет in-memory сессий | ✅ (состояние в `BotSession`, `RateLimit`, `SyncQueue`; на уровне модуля остаются только кэши клиентов и конфигурации) |
| PostgreSQL, Prisma | ✅ 🔎 (pooled `DATABASE_URL`, `DIRECT_URL` для миграций) |
| Webhook защищён и идемпотентен | ✅ |
| Google credentials и OpenAI только на сервере | ✅ |
| `CRON_SECRET`, защита cron | ✅ (`tick` вызывается внешним планировщиком на Hobby — `docs/CRON.md`) |
| Уведомления не дублируются | ✅ |
| Админка, аутентификация, RBAC | ✅ 🔎 |
| Production/Preview разделены | ⚠️ вы задаёте переменные по средам; токены `_PROD`/`_DEV` разведены в коде |
| Сборка Vercel, отсутствие ошибок TypeScript, тесты | 🔎 запустите `npm run lint && npm run typecheck && npm test && npm run build` (в моей среде `npm` недоступен) |
| README с инструкцией деплоя | ✅ |

## Известные пробелы
1. Портфолио (бот и панель), Mini App, кнопка «Позвонить».
2. Не проверено вживую: формат входящих событий MAX (разбор мягкий — проверьте по первым логам), поведение сертификата Минцифры на Vercel, лимиты Google/OpenAI.
3. Тексты `/privacy` и `/terms` — шаблоны для юридической проверки.
